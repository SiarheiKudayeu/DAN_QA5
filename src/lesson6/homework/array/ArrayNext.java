package lesson6.homework.array;

import java.util.Random;

public class ArrayNext {
    public static void main(String[] args) {
      /*  int[] array = new int[5];
        String[] strings = {"One", "Three", "Two", "Text", "Word", "QA"};
        System.out.println(strings[1]);
        //length
        System.out.println("Количество элементов массива " + strings.length);

        for (int i = 0; i < strings.length; i++) {
            System.out.print(strings[i] + " ");
        }*/
/*
        Random random = new Random();
        int randomInt = random.nextInt(-20, 20);
        System.out.println(randomInt);

        int randomNumber = (int) ((Math.random() * 41) - 20);
        System.out.println(randomNumber);
        int[] array = new int[30];
        for (int i = 0; i < array.length; i++){
            array[i] = random.nextInt(-13, 25);
            System.out.print(array[i] + " ");
        }*/

    }
}
